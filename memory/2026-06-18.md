## 2026-06-18
- Patched OpenClaw config to set `gateway.bind` to "lan" via `gateway(action="config.patch", raw="{\"gateway\":{\"bind\":\"lan\"}}")`.
- Planned to restart the OpenClaw gateway to apply the change.
- Next steps: Run `gateway(action="restart")`, ensure Node server stays alive (e.g., using `nohup` or a process manager), and expose port 8000 via firewall or Tailscale Funnel.
